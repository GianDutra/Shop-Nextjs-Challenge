/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: [
            'files.stripe.com', "s3-alpha-sig.figma.com"
        ]
    },

}

module.exports = nextConfig
